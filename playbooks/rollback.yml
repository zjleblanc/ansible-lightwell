---
- name: Roll back the Lightwell demo application to its previous version
  hosts: "env_{{ target_environment }}"
  gather_facts: false

  roles:
    - role: demo.lightwell.rollback
      vars:
        app_environment: "{{ target_environment }}"
